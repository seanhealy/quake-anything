{
  "name": "quake-anything",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && eslint \"dist/**/*.js\" --fix",
    "watch": "tsc -p tsconfig.json --watch",
    "schemas": "glib-compile-schemas schemas/",
    "stage": "bash scripts/stage.sh",
    "lint": "bun run build && eslint \"dist/**/*.js\"",
    "pack": "bun run stage && (cd build && gnome-extensions pack --force --extra-source=LICENSE --extra-source=types.js --extra-source=geometry.js --extra-source=keybindings.js --extra-source=quake-manager.js --extra-source=prefs .) && mv -f build/quake-anything@yccoskun.github.io.shell-extension.zip .",
    "install-ext": "bun run stage && mkdir -p \"$HOME/.local/share/gnome-shell/extensions/quake-anything@yccoskun.github.io\" && rsync -a --delete build/ \"$HOME/.local/share/gnome-shell/extensions/quake-anything@yccoskun.github.io/\""
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@girs/gjs": "^4.0.0",
    "@girs/gnome-shell": "^50.0.0",
    "eslint": "^9.32.0",
    "typescript": "^5.9.2"
  }
}
